import SectionHeader from './SectionHeader';

export default function Branding() {
	return (
		<section>
			<SectionHeader>PetSoft</SectionHeader>
			<p className="text-lg opacity-70">Manage your pet daycare with ease</p>
		</section>
	);
}
